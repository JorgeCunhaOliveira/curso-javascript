function soma(n1 = 0, n2 = 0){//coloco o zero para definir um parâmetro e não dar erro
    return n1 + n2
}

let res = soma(10,35)//forma escrevendo variável
console.log(res)

console.log(soma(3,9))//forma mais simples sem criar variável